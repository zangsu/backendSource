spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa_review
    username: root
    password: 111111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging:
  level:
      org.hibernate.SQL: debug